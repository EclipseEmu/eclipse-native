import SwiftUI

struct SettingsGeneralView: View {
    var body: some View {
        Form {
            Spacer()
        }
        .navigationTitle("General")
    }
}

#Preview {
    SettingsGeneralView()
}
